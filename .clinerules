# .clinerules

# 1. 自动备份
- 修改文件前总是备份到 .backup/，命名规则为 {filename}.{YYYYMMDD-HHmmss}.bak
- 改后展示备份路径和 diff。

# 2. 计划模式强制
- 所有任务默认只计划，不执行。必须等待用户说“执行”才动手。
- 高风险操作（如 rm, force push）需再次确认。

# 3. 自动展示成果
- 使用 git diff 或直接展示改动对比。
- 前端项目尽量启动 dev server 预览。

# 4. 节省 Token
- 只输出必要代码片段。
- 长对话提醒 compact。
- 简单查询不列计划。
- 合并重复输出。

#工作流程
- 在修改任何文件前，先简要说明你的改动计划。
- 如果你需要执行终端命令，请先解释命令的作用。
- 每次任务完成后，列出你所修改的文件清单。

#语言与注释
- 请始终使用中文回复我。

- #完善页面

- 样式：使用 style.css 现有变量，与整体设计一致。
- 布局：使用 .feature-grid 和 .overview-grid，确保移动端自适应。
- 内容：部分文案从 app.js 的 SOLUTION_DATA.charging 里读取，用 JS 动态渲染高亮特性。
